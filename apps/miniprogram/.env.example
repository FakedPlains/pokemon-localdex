# API 地址 —— 复制此文件为 .env 可手动覆盖（.env 已被 .gitignore 排除）
#
# 默认行为（不需要 .env 文件即可工作）：
#   dev:weapp  (--watch) → http://localhost:3030    本地 API 服务
#   build:weapp          → https://pokemon-localdex.pages.dev  Cloudflare Worker
#
# 如需覆盖，取消下方注释并填入实际地址：
# API_BASE_URL=http://localhost:3030
